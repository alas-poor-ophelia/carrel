node_modules/
main.js
styles.css
*.log
.DS_Store
mcp/node_modules/
mcp/screenshots/

# test-vault: a local dev/MCP fixture only — never part of the published plugin
# repo. Kept on disk for the dev loop, fully untracked (the store scanner does
# not ignore a dir named "carrel-test-vault", so it must not be committed).
carrel-test-vault/
